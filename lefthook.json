{
  "$schema": "https://raw.githubusercontent.com/evilmartians/lefthook/master/schema.json",
  "pre-commit": {
    "commands": {
      "format": {
        "run": "npm run format {staged_files}",
        "glob": ["*.{js,jsx,ts,tsx,json,css,html,md}"],
        "stage_fixed": true
      },
      "lint": {
        "run": "npm run lint {staged_files}"
      }
    }
  },
  "commit-msg": {
    "commands": {
      "lint-msg": {
        "run": "npx commitlint --edit {1}"
      }
    }
  }
}
